// import request from 'supertest';
// import 'jest';
// import { Server } from '../app';

// test('router: /', async () => {
// 	// post参数为相对路径，不需要加上http:
// 	const response = await request(Server).post('/');
// 	expect(response.status).toBe(200);
// 	expect(response.text).toBe('hello world');
// });
