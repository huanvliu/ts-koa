模块讲解:

```
        "amqplib": "^0.5.3",      mq库
        "chalk": "^2.4.2",        粉笔输出到控制台有颜色
        "cross-env": "^5.2.0",    跨平台设置env
        "glob": "^7.1.3",         读取文件
        "ioredis": "^4.6.2",      redis驱动
        "koa": "^2.7.0",
        "koa-body": "^4.0.4",     解析body包括文件上传
        "koa-router": "^7.4.0",   koa路由
        "koa-static": "^5.0.0",   koa静态文件
        "moment": "^2.24.0",      时间库
        "mongoose": "^5.4.14",    mongodb库
        "reflect-metadata": "^0.1.13", 反射用来装饰器的时候用到
        "request": "^2.88.0",     request库
        "ts-node": "^7.0.1",      不用编译直接运行ts
        "typescript": "^3.1.6",
        "uuid": "^3.3.2",
        "validate": "^4.5.1"
```

vscode 插件

```
Chinese (Simplified) Language Pack for Visual Studio Code
ESLint
Remote - SSH
Prettier - Code formatter
Open in Browser
Code Runner
```
